#include<stdio.h>
int main()
{
    int num=20,count=0,i;

    for(i=10; i<=20; i++);
    {
    if(i%2==0)
    
        count++;
    
    }
    if(count==0)
         printf("its a not prime number");
    else
         printf("its a prime number");
    return 0;

    
}