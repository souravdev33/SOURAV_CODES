#include<stdio.h>
#include<math.h>
int main ()
{
    float x,result;
    printf("enter the value of x: ");
    scanf("%f",&x);
    result=sqrt(x);
    printf("the result is =%.3f",result);

    return 0;
}