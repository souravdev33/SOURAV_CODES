#include<stdio.h>
#include<math.h>
int main()
{
    double x,result;
    printf("enter the value of x : ");
    scanf("%lf",&x);

    result=cos(x);
    printf("the result is =%lf",result);
    return 0;
}